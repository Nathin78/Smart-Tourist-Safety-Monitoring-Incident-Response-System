spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/tourist_safety?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC}
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=Nathin@1

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

spring.application.name=tourist-safety-system

server.port=8081
server.servlet.context-path=/

jwt.secret=${JWT_SECRET:this_is_a_very_long_secret_key_for_jwt_token_generation_that_is_at_least_32_characters}
jwt.expiration=${JWT_EXPIRATION_MS:86400000}

logging.level.root=INFO
logging.level.com.touristsafety=DEBUG

# Email (configured for Gmail SMTP by default)
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:smarthomeenergymanagement1@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:eiyz guce uozx chrs}
spring.mail.properties.mail.smtp.auth=${SPRING_MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${SPRING_MAIL_SMTP_STARTTLS:true}
spring.mail.properties.mail.smtp.ssl.trust=${SPRING_MAIL_SMTP_SSL_TRUST:smtp.gmail.com}
spring.mail.properties.mail.smtp.connectiontimeout=${SPRING_MAIL_SMTP_CONNECTIONTIMEOUT:5000}
spring.mail.properties.mail.smtp.timeout=${SPRING_MAIL_SMTP_TIMEOUT:5000}
spring.mail.properties.mail.smtp.writetimeout=${SPRING_MAIL_SMTP_WRITETIMEOUT:5000}

# Alert recipient copy (SOS emails go to the user's sos_email first, then this fallback address)
alert.mail.to=${ALERT_MAIL_TO:smarthomeenergymanagement1@gmail.com}
